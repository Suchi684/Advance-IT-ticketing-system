# Server
server.port=8080

# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/ticketing_system?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT
jwt.secret=ecommerce-ticketing-system-jwt-secret-key-that-is-at-least-64-characters-long-for-hs512-algorithm
jwt.expiration-ms=86400000

# Gmail SMTP (for sending)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=itssuchi1202@gmail.com
spring.mail.password=rlfrcble cezoaiir
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Gmail IMAP (for receiving)
mail.imap.host=imap.gmail.com
mail.imap.port=993
mail.imap.username=itssuchi1202@gmail.com
mail.imap.password=rlfrcblecezo aiir

# Scheduler
email.fetch.interval=60000
email.fetch.enabled=true

# CORS
app.cors.allowed-origins=http://localhost:5173

# Scheduling
spring.task.scheduling.pool.size=2
